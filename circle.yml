test:
  override:
    - ./execute_tests.sh

dependencies:
  pre:
    - pip install -r requirement_tests.txt
    - pip install -r requirement_release.txt

deployment:
  default:
    branch: [master]
    commands:
      - ./build_tag.sh
